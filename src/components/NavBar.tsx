import React from 'react';

const NavBar: React.FC = () => {
  return (
    <div>
      <nav className='nav_bar'>
        <span>Help</span>
        <span>Sign Out</span>
      </nav>
    </div>
  );
};

export default NavBar;
